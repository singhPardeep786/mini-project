<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex items-center justify-end">
            <a href="/logout" class="text-decoration-none bg-red-500 text-sm px-3 py-2 rounded-md">Logout</a>
        </div>
        <h5 class="mt-1">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <div class="mt-3">
                <textarea 
                class="w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md mt-2 p-3" 
                name="content" 
                placeholder="What's on your mind ?"
                ><%= post.content %></textarea>
            </div>
            <div>
                <input 
                type="submit" 
                value="Update Post"
                class="px-4 py-2 bg-yellow-500 rounded-md cursor-pointer text-sm text-black"
                >
            </div>
        </form>

        
    </div>
    
</body>
</html>