<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Association</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full h-screen bg-zinc-900 text-white p-10 flex items-center justify-center flex-col">
        <h3 class="capitalize text-3xl tracking-tighter mb-5">create account</h3>
        <form action="/register" method="post" class="border-2 border-zinc-700 p-10 rounded-md">
            <div>
                <input 
                type="text"
                placeholder="name"
                name="name"
                class="px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-500"
                required
                >
            </div>
            <div class="mt-5">
                <input 
                type="text"
                placeholder="username"
                name="username"
                class="px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-500"
                required
                >
            </div>
            <div class="my-5">
                <input 
                type="email"
                placeholder="email"
                name="email"
                class="px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-500"
                required
                >
            </div>
            <div class="my-5">
                <input 
                type="password"
                placeholder="password"
                name="password"
                class="px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-500"
                required
                >
            </div>
            <div>
                <input 
                type="number"
                placeholder="age"
                name="age"
                class="px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-500"
                >
            </div>
            <div class="mt-5">
                <input 
                type="submit"
                value="Create Account"
                class="w-full px-4 py-2 rounded-md outline-none bg-blue-600 border-zinc-500 cursor-pointer"
                >
            </div>
            <h3 class="mt-4 text-center">OR</h3>
            <h3 class="mt-4 text-center text-gray-400">Already have an account ?</h3>
            <a href="/login" class="w-full text-center px-4 py-2 rounded-md outline-none bg-green-600 border-zinc-500 cursor-pointer inline-block mt-1">Log in</a>
        </form>
    </div>
    
</body>
</html>